APP_WORKSPACE="${APP_WORKSPACE:-app/Sunclub.xcworkspace}"
APP_SCHEME="${APP_SCHEME:-Sunclub}"
APP_IDENTIFIER="${APP_IDENTIFIER:-app.peyton.sunclub}"

DEFAULT_SIMULATOR_DEVICE="${DEFAULT_SIMULATOR_DEVICE:-iPhone 17 Pro}"
RUN_SIMULATOR_NAME="${RUN_SIMULATOR_NAME:-Sunclub Run iPhone 17 Pro}"
TEST_SIMULATOR_NAME="${TEST_SIMULATOR_NAME:-Sunclub Test iPhone 17 Pro}"
SCREENSHOT_SIMULATOR_PREFIX="${SCREENSHOT_SIMULATOR_PREFIX:-Sunclub Screenshots}"

RUN_APP_PATH="${RUN_APP_PATH:-Build/Products/Debug-iphonesimulator/Sunclub.app}"
TEST_XCODEBUILD_ARGS="${TEST_XCODEBUILD_ARGS:--parallel-testing-enabled NO -maximum-parallel-testing-workers 1}"

BUILD_DERIVED_DATA="${BUILD_DERIVED_DATA:-.DerivedData/build}"
RUN_DERIVED_DATA="${RUN_DERIVED_DATA:-.DerivedData/run}"
TEST_DERIVED_DATA="${TEST_DERIVED_DATA:-.DerivedData/test}"
SCREENSHOT_DERIVED_DATA="${SCREENSHOT_DERIVED_DATA:-.DerivedData/screenshots}"
ARCHIVE_DERIVED_DATA="${ARCHIVE_DERIVED_DATA:-.DerivedData/archive}"

ARCHIVE_PATH="${ARCHIVE_PATH:-.build/Sunclub.xcarchive}"
EXPORT_PATH="${EXPORT_PATH:-.build/export}"
EXPORT_OPTIONS_PATH="${EXPORT_OPTIONS_PATH:-scripts/appstore/ExportOptions.plist}"
TEAM_ID="${TEAM_ID:-3VDQ4656LX}"

CLOUDKIT_CONTAINER_ID="${CLOUDKIT_CONTAINER_ID:-iCloud.app.peyton.sunclub}"
CLOUDKIT_TEAM_ID="${CLOUDKIT_TEAM_ID:-3VDQ4656LX}"
CLOUDKIT_ENVIRONMENT="${CLOUDKIT_ENVIRONMENT:-development}"
CLOUDKIT_SCHEMA_FILE="${CLOUDKIT_SCHEMA_FILE:-.state/cloudkit/sunclub-cloudkit-schema.json}"
CLOUDKIT_TOKEN_METHOD="${CLOUDKIT_TOKEN_METHOD:-keychain}"
CLOUDKIT_TOKEN_TYPE="${CLOUDKIT_TOKEN_TYPE:-management}"
