# FastVLM Model Assets

Run the download script from the repository root to populate `model/`:

```sh
chmod +x app/get_pretrained_mlx_model.sh
app/get_pretrained_mlx_model.sh --model 0.5b --dest app/Sunclub/FastVLMModel/model
```

The downloaded model is intentionally not committed.
